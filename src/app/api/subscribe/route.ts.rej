diff a/src/app/api/subscribe/route.ts b/src/app/api/subscribe/route.ts	(rejected hunks)
@@ -69,7 +69,10 @@ export async function POST(request: Request) {
     const {user} = session;
     if (!user || !user.id || !user.email) {
       console.error('User data missing in session:', user);
-    const requestBody = (await request.json()) as SubscribeRequestBody;
+    // Safely parse and type the incoming JSON payload
+    // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
+    const requestBody = (await request.json()) as SubscribeRequestBody;
+
     const {
       priceId,
       redirectPath = '/',
